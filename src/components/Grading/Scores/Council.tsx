const Council = (props: {}) => {
  const { values } = props;
  const total = values[5];
  //const handover = values[11];
  //const leadOpportunities = values[12];
  //const plan = values[13];
  return <div>{total}</div>;
};

export default Council;
